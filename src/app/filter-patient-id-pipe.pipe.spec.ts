import { FilterPatientIdPipe } from './filter-patient-id-pipe.pipe';

describe('FilterPatientIdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPatientIdPipe();
    expect(pipe).toBeTruthy();
  });
});
